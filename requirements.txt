# Зависимости Python для стека обхода препятствий Griphonyx
# Установка: pip install -r requirements.txt

# Основная вычислительная математика
numpy>=1.24.0

# Научные вычисления (опционально, для продвинутых функций)
scipy>=1.10.0

# Опционально: ускоренная кластеризация DBSCAN (если недоступен — встроенная реализация)
# scikit-learn>=1.0.0

# Опционально: обнаружение объектов YOLO (обнаружение препятствий по камере)
# ultralytics>=8.0.0      # YOLOv8  (pip install ultralytics)
# opencv-python>=4.5.0    # предобработка кадров с камеры

# Опционально: вспомогательные маркеры RViz/rviz2 (только визуализация)
# transforms3d>=0.3.1
